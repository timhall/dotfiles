#
# Secrets
#

# (in .zprofile, which chezmoi seeds once and never overwrites)

#
# PATH
#

# (moved to .zshenv so agents / non-interactive shells resolve these too)

#
# Aliases
#

alias rosetta="env /usr/bin/arch -x86_64 /bin/zsh --login"

#
# nvm
#

# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
# [ -s "$NVM_DIR/bash_completion" ] && \. "$NVM_DIR/bash_completion"

#
# pyenv (PATH in .zshenv; shell integration here)
#

eval "$(pyenv init -)"

#
# bun completions (PATH in .zshenv)
#

[ -s "$HOME/.bun/_bun" ] && source "$HOME/.bun/_bun"

# fnm — PATH + `fnm env` now in .zshenv (covers interactive + agent shells)
